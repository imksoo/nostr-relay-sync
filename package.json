{
  "name": "nostr-relay-sync",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "docker build . -t nostr-relay-sync",
    "dev": "npm run build && docker run --rm -it nostr-relay-sync"
  },
  "author": "Kirino Minato <kirino.minato@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "nostr-fetch-root": "github:jiftechnify/nostr-fetch",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "ts-node": "^10.9.1"
  }
}
